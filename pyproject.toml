[project]
name = "calendar-tasks"
version = "0.1.1"
description = "Creates an .ics file with recurring apointments"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]",
    "holidays",
    "icalendar",
    "pydantic",
    "pytest",
    "python-dateutil",
]
